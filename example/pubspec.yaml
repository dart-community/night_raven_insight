name: night_raven_insight_example
description: An example package that uses night_raven_insight for analysis.
version: 3.0.0
publish_to: none

environment:
  sdk: ^3.6.0

dev_dependencies:
  night_raven_insight: ^3.0.0
